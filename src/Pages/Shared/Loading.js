import React from 'react';

const Loading = () => {
    return (
        <div>
            <div className=' w-full h-screen flex justify-center items-center'><button class="btn loading ">loading</button></div>
        </div>
    );
};

export default Loading;